# Agent notes

Read [`ARCHITECTURE.md`](ARCHITECTURE.md) first: it maps every module, command, hook and exported
item. Update it in the same commit as any change that adds, removes, renames or moves something it
lists.
