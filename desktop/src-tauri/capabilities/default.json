{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shiver's own shell UI. Server webviews are deliberately absent: a Sharkord page gets no access to any Shiver command.",
  "windows": ["main"],
  "webviews": ["shell", "overlay", "popup"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "opener:default"
  ]
}
