{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Shiver's own webviews (shell, bell, popup). Server webviews are not listed, so no plugin permission reaches a Sharkord page.",
  "webviews": [
    "shell",
    "overlay",
    "popup"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging"
  ]
}
