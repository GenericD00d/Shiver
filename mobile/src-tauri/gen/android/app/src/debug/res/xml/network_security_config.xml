<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only: the vite dev server is served over http (10.0.2.2 or the host's LAN address). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
