{
  "name": "shiver-mobile",
  "private": true,
  "version": "0.1.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:bridge": "vite build --config vite.bridge.config.ts && vite build --config vite.bridge.config.ts --mode document-start",
    "build": "bun run build:bridge && tsc --noEmit && vite build",
    "tauri": "tauri",
    "app": "tauri dev",
    "android": "tauri android dev",
    "android:build": "tauri android build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@types/node": "^26.4.1",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
